name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  native-tests:
    name: Tests (${{ matrix.architecture }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-15
            architecture: arm64
          - runner: macos-15-intel
            architecture: x86_64
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Show toolchain and architecture
        run: |
          swift --version
          uname -m

      - name: Validate package manifest
        run: swift package dump-package >/dev/null

      - name: Run tests
        run: swift test

  universal-build:
    name: Universal app bundle
    needs: native-tests
    runs-on: macos-15
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Validate packaging files
        shell: bash
        run: |
          bash -n Scripts/*.sh
          plutil -lint Packaging/Info.plist Packaging/Countpane.entitlements

      - name: Build release app
        run: ./Scripts/build-app.sh 0.0.0-ci

      - name: Verify universal executable
        shell: bash
        run: |
          ARCHS="$(lipo -archs dist/Countpane.app/Contents/MacOS/Countpane)"
          echo "Architectures: $ARCHS"
          [[ " $ARCHS " == *" arm64 "* ]]
          [[ " $ARCHS " == *" x86_64 "* ]]
          test -f dist/Countpane.app/Contents/Resources/AppIcon.png
          codesign --verify --deep --strict --verbose=2 dist/Countpane.app

      - name: Upload app bundle
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: Countpane-universal-app
          path: dist/Countpane.app
          if-no-files-found: error
